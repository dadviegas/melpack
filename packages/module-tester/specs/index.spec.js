import src from 'src/index.js'

describe('action', function () {
  it('test', function () {
    expect(src).toEqual(4)
  })
})

describe('action', function () {
  it('test', function () {
    expect(1).toEqual(1)
  })
})

describe('action', function () {
  it('test', function () {
    expect(1).toEqual(1)
  })
})

describe('action', function () {
  it('test', function () {
    expect(1).toEqual(1)
  })
})

describe('action', function () {
  it('test', function () {
    expect(1).toEqual(1)
  })
})

describe('action', function () {
  it('test', function () {
    expect(1).toEqual(1)
  })
})

describe('action2', function () {
  it('test', function () {
    expect(1).toEqual(1)
  })
})

describe('action1', function () {
  it('test', function () {
    expect(1).toEqual(1)
  })
})
