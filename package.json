{
  "lerna": "^2.0.0-rc.3",
  "version": "1.0.0",
  "scripts": {
    "compile": "lerna run compile",
    "build": "lerna bootstrap",
    "clean": "lerna clean && rm -rf node_modules",
    "publish": "lerna publish",
    "publish:conventional": "lerna publish --git-remote upstream --conventional-commits",
    "publish:git": "lerna publish --yes --skip-npm"
  },
  "engines": {
    "node": ">6.0"
  },
  "author": "DadViegas",
  "repository": {
    "type": "git",
    "url": "https://github.com/dadviegas/melpack"
  },
  "devDependencies": {
    "babel-core": "^6.24.0",
    "babel-loader": "^7.0.0-beta.1",
    "lerna": "^2.0.0-rc.3"
  }
}
