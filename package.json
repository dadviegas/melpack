{
  "lerna": "2.0.0-beta.23",
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "2.0.0-beta.38"
  },
  "scripts": {
    "build": "lerna bootstrap --hoist",
    "clean": "lerna clean && rm -rf node_modules"
  }
}
